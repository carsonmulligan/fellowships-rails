<!-- Premium Features Modal -->
<div id="premium-features-modal" class="modal" style="display: none;">
  <div class="modal-content">
    <div class="modal-grid">
      <!-- Left Panel (Scrollable) -->
      <div class="left-panel">
        <div class="landing-content">
          <%= render 'shared/landing_content' %>
        </div>
      </div>

      <!-- Right Panel -->
      <div class="right-panel">
        <!-- Video Section -->
        <div class="video-container">
          <iframe 
            width="100%" 
            height="315" 
            src="https://www.youtube.com/embed/UVtrvArSihQ" 
            frameborder="0" 
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
            allowfullscreen>
          </iframe>
        </div>

        <!-- Features List -->
        <div class="features-list">
          <h3>🔒 Premium Features</h3>
          <ul>
            <li>⭐️ Bookmark Scholarships</li>
            <li>📝 Track Applications</li>
            <li>🔔 Get Notifications</li>
            <li>💬 Join Community Chat</li>
          </ul>
        </div>

        <!-- Auth & Pricing -->
        <div class="auth-section">
          <button id="google-sign-in" class="google-btn">
            <img src="/google-icon.png" alt="Google">
            Sign in with Google
          </button>
          <div class="pricing">
            <span class="price">$9.99/month</span>
            <button id="subscribe-btn" class="subscribe-btn">Subscribe Now</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.7);
  z-index: 1000;
}

.modal-content {
  position: relative;
  width: 90%;
  max-width: 1200px;
  margin: 2% auto;
  background: white;
  border-radius: 12px;
  padding: 20px;
  max-height: 90vh;
}

.modal-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  height: 100%;
}

.left-panel {
  overflow-y: auto;
  padding-right: 20px;
  border-right: 1px solid #eee;
}

.right-panel {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.video-container {
  width: 100%;
  border-radius: 8px;
  overflow: hidden;
}

.features-list {
  background: #f8f9fa;
  padding: 20px;
  border-radius: 8px;
}

.features-list ul {
  list-style: none;
  padding: 0;
}

.features-list li {
  margin: 10px 0;
  font-size: 1.1em;
}

.auth-section {
  display: flex;
  flex-direction: column;
  gap: 15px;
  align-items: center;
}

.google-btn {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 24px;
  border: 1px solid #ddd;
  border-radius: 24px;
  background: white;
  cursor: pointer;
  transition: all 0.2s;
}

.google-btn:hover {
  background: #f8f9fa;
}

.pricing {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
}

.price {
  font-size: 1.5em;
  font-weight: bold;
}

.subscribe-btn {
  padding: 12px 36px;
  background: #007bff;
  color: white;
  border: none;
  border-radius: 24px;
  font-size: 1.1em;
  cursor: pointer;
  transition: all 0.2s;
}

.subscribe-btn:hover {
  background: #0056b3;
}
</style> 